#include<stdio.h>
#include<math.h>
int pow(int , int );
int main()
{
    int a = 10;
    int b = 20;
    print("%d",sq);
    pow(p,q);

}
   int pow(int p, int q)
   for (int i =1; i<p; i++){
   sq = p*q;
}
